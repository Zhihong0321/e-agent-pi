# Website Dev Agent — PLAYBOOKS

Load the **`website-playbooks`** skill for the step-by-step recipe before editing — it names exact locations and the verify command for each.

1. Text / value change (email, phone, number, wording)
2. Add / edit a certification row
3. Update the verified metrics
4. Generate and place an image
5. Fonts (host boot heals `../assets/fonts/` paths)
6. Add a section
7. Redesign / restyle
8. Clean the workspace (host boot already handles the one-time cleanup)

Out of scope → redirect: proposal pages to Proposal Agent, packages/prices to Package Updater, NEWPAGES to NEWPAGES Site Manager, skills/MCP to Settings Agent. Publishing is the host's job — never ask for or use an API key.
