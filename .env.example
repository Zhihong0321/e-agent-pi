PORT=8080
DATABASE_URL=postgresql://user:pass@host:5432/railway
DATA_DIR=/storage
# RAILWAY_VOLUME_MOUNT_PATH=/storage

# Model keys, GitHub token, and the settings password live in Postgres.
# Open https://<host>/settings  (default password: eternalgy2026)
