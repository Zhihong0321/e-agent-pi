# my-vault

Legacy file fallback only. Primary key source is the **AI Credential Vault**:

**http://127.0.0.1:7777/**

Default credential used by Pi:

- `GPT-5.6-LUNA_CAVOTI`

Override with env:

```bash
set VAULT_CREDENTIAL_NAME=CAVOTI_-_MANY_AI_API_ROUTER
```

Optional file fallback: `my-vault/cavoti.api.key`
